import React, { Component } from 'react'
import {
    EuiButton
  } from '@elastic/eui';

export default class Home extends Component {
    render() {
        return (
            <div>
                 <EuiButton fill>
          Primary
        </EuiButton>
            </div>
        )
    }
}
